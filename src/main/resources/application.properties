# spring-dotenv's initializer must be registered here (not application.yml) since it runs
# before YAML property sources are processed - it loads .env into the Spring Environment.
context.initializer.classes=me.paulschwarz.springdotenv.spring.DotenvApplicationInitializer
